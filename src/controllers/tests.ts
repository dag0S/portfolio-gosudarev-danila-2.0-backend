import { Request, Response } from "express";

/**
 * @route POST /api/tests/upload
 * @desc Upload file
 * @access Public
 */
export const uploadFile = async (
  req: Request,
  res: Response
): Promise<any> => {};
